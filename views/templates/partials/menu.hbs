<section id="menu">
  <section>
    <ul class="links">
      {{#each this as |category|}}
        <li>
          <a href="/?subcategory={{category.name}}">
            <h3>
              {{category.name}}
            </h3>
            <p>
              {{category.description}}
            </p>
          </a>
        </li>
      {{/each}}
    </ul>
  </section>
</section>
