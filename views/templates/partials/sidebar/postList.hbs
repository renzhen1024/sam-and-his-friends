<li>
	<article>
		<header>
			<h3>
				<a href="{{originPost}}">
					{{title}}
				</a>
			</h3>
			<time class="published" datetime="{{date}}">
				{{dateFormatter date}}
			</time>
		</header>
		<a href="{{originPost}}" class="image">
			<img src="{{authorImageUrl}}" alt="" />
		</a>
	</article>
</li>
